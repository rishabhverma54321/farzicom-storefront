import { styled } from "@styles/themes";

export const RichTextContentContainer = styled.div`
  max-width: 1140px;

  color: #444;
  
  line-height: 22.54px;

  font-style: normal;
  font-weight: normal;
  font-size: 14px;
`;
